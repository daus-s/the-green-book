function sortByDate(list, fieldName) {
    if (!Array.isArray(list)) {
        return;
    }
}

function sortByDate(list) {
    if (!Array.isArray(list)) {
        return;
    }
    sortByDate(list, "creation_time");
}
